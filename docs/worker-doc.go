package docs

/*
workers :
1. would be registered on a smart contract with their public key and staked amount
2. commnad wnode --reg [containerID] to register a container
3. on the registration, the worker node deterimnes wether the code inside the container is deterministic or not.
workers should only be able to accept inbound connections from gateway nodes.
*/
